<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:focusableInTouchMode="true"
    tools:context="com.mnandanuri.mytodoapp.EditItemActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="10"
        android:selectAllOnFocus="true"
        android:id="@+id/etMultiLineText"
        android:layout_alignParentTop="true"
        android:layout_alignEnd="@+id/btnCancel" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_save"
        android:id="@+id/btnSaveNewText"
        android:onClick="onSave"
        android:hint="Save"
        android:layout_alignTop="@+id/btnCancel"
        android:layout_alignStart="@+id/etMultiLineText" />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btnCancel"
        android:id="@+id/btnCancel"
        android:onClick="onCancel"
        android:layout_below="@+id/etMultiLineText"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="85dp" />

</RelativeLayout>
